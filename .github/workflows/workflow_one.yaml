name: github workflow

on:
    push:
        branches:
            - main
    workflow_dispatch:

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - name: print message
              run: echo Ready to begin